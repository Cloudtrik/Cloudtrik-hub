name: Release
on:
  push:
    tags: ["v*"]

permissions:
  id-token: write    # OIDC for npm provenance
  contents: write    # for GitHub release

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }
      - uses: pnpm/action-setup@v4
        with: { version: '9' }
      - uses: actions/setup-node@v6
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - run: pnpm build
      - run: pnpm test
      - run: pnpm lint
      - run: pnpm typecheck
      - name: Verify version matches tag
        run: |
          PKG_VER=$(node -p "require('./package.json').version")
          TAG_VER="${GITHUB_REF_NAME#v}"
          if [ "$PKG_VER" != "$TAG_VER" ]; then
            echo "Mismatch: package=$PKG_VER tag=$TAG_VER"; exit 1
          fi
      - name: Upgrade npm to ≥11.5.1 (Trusted Publisher OIDC support)
        run: npm install -g npm@latest
      - name: Publish to npm via OIDC Trusted Publisher
        # No NODE_AUTH_TOKEN — npm CLI uses OIDC token exchange when:
        # (a) id-token: write permission is granted (above)
        # (b) Trusted Publisher is configured server-side on the package
        # (c) no _authToken in ~/.npmrc (we intentionally do not set NPM_TOKEN env)
        run: npm publish --provenance --access public
      - uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          body_path: CHANGELOG.md
